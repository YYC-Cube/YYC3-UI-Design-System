name: "YYC³ Design System CI/CD (Backup - Disabled)"

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: "Disabled"
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use ci-cd.yml instead."
